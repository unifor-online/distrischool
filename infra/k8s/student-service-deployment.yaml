apiVersion: apps/v1
kind: Deployment
metadata:
  name: student-service
  namespace: distrischool
spec:
  replicas: 2
  selector:
    matchLabels:
      app: student-service
  template:
    metadata:
      labels:
        app: student-service
    spec:
      containers:
      - name: student-service
        image: distrischool/student-service:0.1.0
        ports:
        - containerPort: 8080
        env:
        - name: SPRING_DATASOURCE_URL
          value: jdbc:postgresql://postgres-svc.distrischool.svc.cluster.local:5432/distrischool
        envFrom:
        - secretRef:
            name: db-secret
