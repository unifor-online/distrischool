apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-staff-service
  namespace: distrischool
spec:
  replicas: 2
  selector:
    matchLabels:
      app: admin-staff-service
  template:
    metadata:
      labels:
        app: admin-staff-service
    spec:
      containers:
      - name: admin-staff-service
        image: distrischool/admin-staff-service:0.1.0
        ports:
        - containerPort: 8080
        env:
        - name: SPRING_DATASOURCE_URL
          value: jdbc:postgresql://postgres-svc.distrischool.svc.cluster.local:5432/distrischool
        envFrom:
        - secretRef:
            name: db-secret
